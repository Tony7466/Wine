description: fix formatting calls that might overflow (gcc -Wformat-overflow)
author: Michael Gilbert <mgilbert@debian.org>

--- a/tools/winedump/msc.c
+++ b/tools/winedump/msc.c
@@ -462,7 +462,7 @@ static const char* get_callconv(unsigned
     case CV_CALL_RESERVED:      callconv = "reserved"; break;
     default:
         {
-            static char tmp[16];
+            static char tmp[18];
             sprintf(tmp, "callconv=%x", cc);
             callconv = tmp;
         }
--- a/programs/winedbg/tgt_active.c
+++ b/programs/winedbg/tgt_active.c
@@ -152,6 +152,7 @@ static BOOL dbg_exception_prolog(BOOL is
 
         switch (addr.Mode)
         {
+        default: name = "unknown"; break;
         case AddrMode1616: name = "16 bit";     break;
         case AddrMode1632: name = "segmented 32 bit"; break;
         case AddrModeReal: name = "vm86";       break;
--- a/dlls/krnl386.exe16/vxd.c
+++ b/dlls/krnl386.exe16/vxd.c
@@ -1318,7 +1318,7 @@ void WINAPI __wine_vxd_win32s( CONTEXT *
 
         if (!atom || GlobalGetAtomNameA(atom, name, sizeof(name)))
         {
-            TRACE("NtCreateSection: name=%s\n", atom? name : NULL);
+            TRACE("NtCreateSection: name=%s\n", atom? name : "NULL");
 
             result = CreateFileMappingA(hFile, NULL, protect,
                                           size? size->u.HighPart : 0,
